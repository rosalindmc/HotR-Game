//colourTiles()
//Sets the colour of tiles if they are in walk or run range

var tile, move

tile = argument0
move = argument1

if tile.g > move
{
    tile.overlay = 2
}
else
{
    tile.overlay = 1
}
